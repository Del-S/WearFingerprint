package cz.uhk.fim.kikm.wearnavigation.utils;

import android.content.Context;
import android.support.constraint.ConstraintLayout;
import android.view.LayoutInflater;
import android.view.View;

import cz.uhk.fim.kikm.wearnavigation.R;

public class MarkerOptions extends ConstraintLayout {
    public MarkerOptions(Context context) {
        super(context);

        LayoutInflater inflater = LayoutInflater.from(context);
        //View view = inflater.inflate(R.layout.actions_marker, );
        //addView(view);
    }
}
